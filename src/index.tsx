import React from 'react';
import ReactDOM from 'react-dom/client';
import { DemoApp } from './dev/app';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(<DemoApp />);
